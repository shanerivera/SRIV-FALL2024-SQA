#!/bin/bash

# Check if the bandit report exists
if [ -f "bandit_report.csv" ]; then
  echo "Adding Bandit report..."
  git add bandit_report.csv
  git commit -m "Add/update Bandit report"
  git push
else
  echo "No Bandit report found, skipping push."
fi

